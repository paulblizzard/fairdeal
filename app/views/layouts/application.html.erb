<!DOCTYPE html>
<html>
  <head>
    <title>Fairdeal</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% if user_signed_in? %>
      <div class="auth_links">
        Signed in as <%= current_user.email %> |
          <%= link_to "Sign out", :destroy_user_session, method: :delete %> 
        </div>
    <% else %>
      <div class="auth_links">
        <%= link_to "Sign in", :new_user_session, method: :get %> 
        <%= link_to "Register", :new_user_registration, method: :get %> 
      </div>
    <% end %>

    <p class="notice"><%= notice %></p>
    <% if notice.nil? %><p class="alert"><%= alert %></p><% end %>
    <%= yield %>
  </body>
</html>
