<%= form_for(dealing) do |f| %>
  <% if dealing.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dealing.errors.count, "error") %> prohibited this dealing from being saved:</h2>

      <ul>
      <% dealing.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :due_date %>
    <%= f.date_select :due_date %>
  </div>

  <div class="field">
      <%= f.label :status %>
      <%= f.select(:status, options_for_select([['In Progress', 1], ['Complete', 2]], 1))%>
  </div>

  <div class="field">
      <%= f.label :category %>
      <%= f.select(:status, options_for_select([['Fair', 1], ['Not Fair', 2], ['Unknown', 3]], 3))%>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
